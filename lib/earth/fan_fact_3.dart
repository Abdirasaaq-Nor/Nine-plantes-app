import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class fun_facts_3 extends StatelessWidget {
  const fun_facts_3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('from fun facts 3'),
    );
  }
}